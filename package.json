{
  "dependencies": {
    "@project-serum/anchor": "^0.16.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3"
  },
  "scripts": {
    "start": "node index.js",
    "build": "cd app && npm install && npm run build"
  },
  "engines": {
    "node": "v16.7.0"
  }
}
